spring:

#  datasource:
#    driver-class-name: org.postgresql.Driver
#    username: postgres
#    password: postgres
#    url: 'jdbc:postgresql://localhost:5432/grafana_readyapi?createDatabaseIfNotExist=true'

  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: update
logging:
  level:
    org:
      hibernate:
        type: TRACE